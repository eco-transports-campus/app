// React
import React, { Component, PropTypes } from 'react';
import Helmet from 'react-helmet';

// MaterialUI Stuff
import { withStyles } from 'material-ui/styles';

// Style
import { styles } from './<%= helpers.capitalize(name) %>PageStyle';


class <%= helpers.capitalize(name) %>Page extends Component {
  constructor(props) {
    super(props);
	}

  render() {
    const { classes } = this.props;

    return (
      <div>
        <Helmet title="<%= helpers.capitalize(name) %>" />
        <%= helpers.capitalize(name) %> page - TODO
      </div>
    );
  }
}

<%= helpers.capitalize(name) %>Page.propTypes = {
  classes: PropTypes.object.isRequired,
};

export default withStyles(styles)(<%= helpers.capitalize(name) %>Page);
